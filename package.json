{
  "name": "reservation",
  "version": "1.0.0",
  "description": "a microservice to make reservations",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --exec 'node -r dotenv/config' src/index.js --watch src",
    "dev": "nodemon --exec 'node -r dotenv/config' src/index.js --watch src"
  },
  "author": "Angel Ulise Tabares Gonzalez <taho2509@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.2.0",
    "events": "^3.0.0",
    "mongodb": "^3.1.13",
    "nats": "^1.2.2",
    "node-nats-streaming": "0.0.51",
    "nodemon": "^1.18.10"
  }
}
